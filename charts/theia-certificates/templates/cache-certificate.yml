apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cache-cert
spec:
  secretName: cache-cert-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "{{ .Values.hosts.configuration.cache }}.{{ .Values.hosts.configuration.baseHost }}"
  dnsNames:
    - "{{ .Values.hosts.configuration.cache }}.{{ .Values.hosts.configuration.baseHost }}"
  privateKey:
    rotationPolicy: Never
